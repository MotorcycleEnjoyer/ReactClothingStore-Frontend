export default function Register(){
    return(
        <>
            <h1>Register account!</h1>
            <form>
            </form>
            <button onClick={()=>{window.location="/"}}>Home</button>
        </>
    )
}